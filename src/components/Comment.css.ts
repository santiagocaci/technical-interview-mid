import { style } from '@vanilla-extract/css';

export const hacknewsComment = style({
  color: 'burlywood',
});

export const hacknewsCommentText = style({
  margin: '0',
  // wordBreak: 'break-all',
  overflowWrap: 'break-word',
});
